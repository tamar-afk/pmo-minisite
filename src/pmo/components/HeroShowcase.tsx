/**
 * Replace with final hero art when ready.
 */
export function HeroShowcase() {
  return (
    <div
      role="region"
      aria-label="Hero image placeholder"
      className="mx-auto flex aspect-video w-full max-w-[min(100%,640px)] min-h-[160px] items-center justify-center rounded-[12px] border border-[rgba(15,15,20,0.2)] bg-black font-mono text-sm font-semibold uppercase tracking-[0.08em] text-white/90 sm:min-h-[180px]"
    >
      HERO IMAGE PLACEHOLDER
    </div>
  )
}
